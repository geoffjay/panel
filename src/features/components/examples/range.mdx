import { Range } from "~components/dashboard";

## Range

### Basic Usage

<Range {...props} />

#### Configuration

```json
{
  "children": [
    {
      "type": "range",
      "properties": {
        "start": 0,
        "end": 100,
        "step": 1,
        "default-value": 50
      }
    }
  ]
}
```
